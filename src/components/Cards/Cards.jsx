import React from "react";
import { Card, CardContent, Typography, Grid } from "@material-ui/core";
const Cards = (props) => {
    return <div className="contanier">Cards</div>
}
export default Cards;